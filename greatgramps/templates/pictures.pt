<div metal:use-macro="layout">
  <div metal:fill-slot="content" tal:omit-tag="1">
    <article class="person-detail">
      <tal:block metal:use-macro="person_header"></tal:block>
      <nav class="person-tabs">
        <a href="${base}people/${person['gramps_id']}/">Profile</a>
        <a href="${base}people/${person['gramps_id']}/ancestors/">Ancestors <span class="tab-count">(${format(num_ancestors, ',')})</span></a>
        <a href="${base}people/${person['gramps_id']}/descendants/">Descendants <span class="tab-count">(${format(num_descendants, ',')})</span></a>
        <span class="active">Pictures <span class="tab-count">(${format(num_pictures, ',')})</span></span>
        <a tal:condition="num_more" href="${base}people/${person['gramps_id']}/more/">More</a>
        <a tal:condition="'explore' not in excluded_pages" href="${base}explore/#focus=${person['gramps_id']}" class="tab-explore">Explore</a>
      </nav>
      <div class="pictures-grid">
        <a tal:repeat="pic pictures" href="${pic['page_url']}" class="picture-item">
          <img src="${pic['url']}" alt="${pic['description'] or pic['page_label']}">
          <span class="picture-caption">
            ${pic['page_label']}
            <span tal:condition="pic['year']" class="picture-year">${pic['year']}</span>
          </span>
        </a>
      </div>
    </article>
  </div>
</div>
