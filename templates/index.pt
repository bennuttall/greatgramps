<div metal:use-macro="layout">
  <div metal:fill-slot="content" tal:omit-tag="1">
    <h1>Family Tree</h1>

    <div class="summary-stats">
      <div class="stat">
        <span class="stat-value">${summary['total_people']}</span>
        <span class="stat-label">people</span>
      </div>
      <div class="stat">
        <span class="stat-value">${summary['total_ancestors']}</span>
        <span class="stat-label">ancestors</span>
      </div>
      <div class="stat">
        <span class="stat-value">${summary['total_places']}</span>
        <span class="stat-label">places</span>
      </div>
      <div class="stat" tal:condition="summary['year_from']">
        <span class="stat-value">${summary['year_from']}–${summary['year_to']}</span>
        <span class="stat-label">records</span>
      </div>
    </div>

    <div class="summary-grid">
      <section>
        <h2>Top surnames</h2>
        <table class="summary-table">
          <tr tal:repeat="item summary['top_surnames']">
            <td><a href="surnames/${item[2]}/">${item[0]}</a></td>
            <td class="year">${item[1]}</td>
          </tr>
        </table>
      </section>

      <section>
        <h2>Top first names</h2>
        <table class="summary-table">
          <tr tal:repeat="item summary['top_given']">
            <td>${item[0]}</td>
            <td class="year">${item[1]}</td>
          </tr>
        </table>
      </section>

      <section class="full-width">
        <h2>Ancestors</h2>
        <table class="summary-table">
          <tr tal:repeat="gen_group summary['generations']">
            <td>
              <span tal:condition="gen_group['gen'] == 0">
                <a href="people/${me_id}/">Me</a>
              </span>
              <span tal:condition="gen_group['gen'] == 1">Parents</span>
              <span tal:condition="gen_group['gen'] == 2">Grandparents</span>
              <span tal:condition="gen_group['gen'] == 3">Great-grandparents</span>
              <span tal:condition="gen_group['gen'] == 4">2nd great-grandparents</span>
              <span tal:condition="gen_group['gen'] &gt; 4">${gen_group['gen'] - 1}th great-grandparents</span>
            </td>
            <td class="year" tal:condition="gen_group['gen'] == 0">1</td>
            <td class="year" tal:condition="gen_group['gen'] != 0">
              ${len(gen_group['people'])} / ${2 ** gen_group['gen']}
              <span tal:define="pct len(gen_group['people']) * 100 // (2 ** gen_group['gen'])"
                    tal:attributes="class 'pct-high' if pct >= 75 else ('pct-mid' if pct >= 50 else 'pct-low')">
                ${pct}%
              </span>
            </td>
          </tr>
        </table>
      </section>
    </div>
  </div>
</div>
